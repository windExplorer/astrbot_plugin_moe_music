# 更新日志

本文件记录 astrbot_plugin_moe_music 的版本变更，最新版本在最上方。

## v0.11.8 - 2026-09-18

### 新增

- **热评按曲目所在平台直取**：网易云之外，酷我（`ncomment.kuwo.cn` 老客户端接口，免签名）与酷狗（`rank/topliked`，参数 md5 签名，盐值与洛雪桌面端一致）的歌曲也能带上本平台热评，卡片标签会标明来源（`热评 · 酷我` / `热评 · 网易云`…）。QQ音乐评论需要额外的 songId 映射与签名，暂不直取——继续走「网易云同曲映射」兜底；本平台抓不到时同样回退映射，映射也失败则不显示。缓存新增 `hot_comment_source` 字段（旧缓存库自动迁移补列）。
- **引用分享 + `下载` / `歌词`**：`下载` 是「点歌文件」的短别名（`下载 <歌名>` 也可直接用）；`歌词` 是「查歌词」的短别名。两者都支持**引用歌曲分享卡片或分享链接**后不带歌名直接用——`下载` 下载那首歌的文件本体，`歌词` 发那首歌的歌词（图片渲染，失败回退文本）。

### 说明

- 酷狗热评以歌曲 hash 为键：分享链接里带 hash 时能精确命中；曲目来自搜索（id 是 AudioID 而非 hash）时接口大概率查不到，此时自动回退网易云映射。
- `下载` / `歌词` 属于比较通用的命令词，若与其他插件注册的指令撞名会同时触发，介意的话请继续使用 `点歌文件` / `查歌词`。

## v0.11.7 - 2026-09-18

### 新增

- **歌曲信息卡片**（默认开）：点歌成功后，先发一张竖版信息卡片（图片），再发语音 / 文件 / 音乐卡片。覆盖点歌、点歌文件、分享识别与 AI 点歌全部路径；在**取到播放链接之后、音频之前**发送，不会出现「有卡片没歌」。
  - **基础信息**：封面（大图圆角）/ 歌名 / 歌手 / 专辑 / 时长 / 音源平台 / 本次音质 / 点歌人 / 时间，任何情况下都画得出来；
  - **增强信息**（缺失自动省略，整块不画）：发行年份（网易云公开接口）、热门评论第一条（同上）、歌手简介（当前对话模型生成，标注「AI 生成」）；
  - **越用越全**：增强信息抓完只写缓存（`records.db` 新增 `song_meta` / `artist_meta` 两张表），不拖慢发歌——第一次点某首歌是基础卡片，第二遍起就是完整卡片；**歌手简介按歌手维度复用**，同一位歌手只生成一次；
  - **失败都不致命**：年份/评论接口被限流、LLM 不可用、封面下载失败、渲染失败，全部只记日志，卡片降级为基础信息或干脆不发，歌照常发。

### 新增配置（5 项，控制台「歌曲信息卡片」分组）

| 键 | 默认 | 说明 |
|---|---|---|
| `song_card_enable` | 开 | 是否发送信息卡片 |
| `song_card_year` | 开 | 补发行年份（网易云公开接口） |
| `song_card_comment` | **关** | 补网易云热评第一条（未加密旧接口，可能被限流） |
| `song_card_artist_bio` | 开 | LLM 生成歌手简介（无可用模型时自动跳过） |
| `song_card_repeat_sec` | 300 | 同一会话同一首歌在此秒数内不重复发卡片（0 = 每次都发） |

### 设计取舍

- **异步补齐而不是现场等待**：年份/评论/简介首次要联网 + LLM（2~8 秒），等它们会把「点歌后多久出声」拖慢一大截。现改为卡片即时发（只读缓存），抓取放后台任务，插件卸载时统一取消；抓取失败会记「尝试时间」，**7 天内不重复打接口**（负缓存），避免每点一次就打一遍永远抓不到的接口。
- **非网易云曲目的同曲映射要求严格**：歌名归一化后必须完全一致、歌手也要吻合才采用，宁可没有年份/热评，也不把别的歌的评论挂上来。
- **评论接口只接网易云旧版未加密端点**：lx-music-desktop 的新版评论走 weapi 加密（AES+RSA），移植成本高且随网易云改动失效；旧端点失败即静默跳过（因此默认关）。
- AI 生成的歌手简介明确标注「AI 生成」；模型回复「暂无资料 / 不确定」时视为无简介。

## v0.11.6 - 2026-09-18

### 修复

- **下载文件的扩展名按「请求音质」推断，上游却可能给另一种容器**：线上报错
  `mutagen.flac.FLACNoHeaderError: '…- HOYO-MiX.flac' is not a valid FLAC file`
  ——以 `flac` 下载（`点歌文件` / 引用分享下载）时，上游（QQ音乐无损链接实测）回的
  其实是 m4a，插件却按 `.flac` 落盘并用 FLAC 写入器嵌标签。现改为**容器以文件内容为准**：
  - 新增 `core/audio_probe.py::sniff_audio_ext()`：按文件头识别容器（`fLaC` / `ID3`
    （含「ID3 标签 + fLaC」）/ MPEG 帧同步字 / `ftyp` / `OggS` / `RIFF`+`WAVE` /
    `MAC ` / `wvpk` / ASF / `#!AMR`），Content-Type 仅作兜底，认不出返回 `None`；
  - 下载完成后按真实容器校正扩展名（`song.flac` → `song.m4a`），日志写明「预期 X、
    实际 Y、Content-Type、大小」，发给用户/协议端的文件名与真实格式一致；
- **元数据嵌入按内容分派写入器**（`core/metadata.py`）：扩展名与真实容器不符时按真实
  容器写（并告警）；真实容器不在支持列表（wav / ape / ogg 等）时**跳过而不硬写**——
  这正是本次报错的根因（拿 FLAC 写入器去写 m4a）。嵌入失败的日志现在还带上容器判定、
  文件大小与文件头十六进制。
- 下载内容为 **0 字节**直接判失败（不再把空文件发给用户）；容器识别不出来时保留原扩展名
  并打出文件头十六进制，便于一眼区分「加密文件」与「上游错误页」。

### 说明

- 本次只修「容器识别 / 命名 / 写入器分派」，**不改音质选择逻辑**：若日志出现「无法识别
  下载内容的音频容器」，说明该音质的上游内容不是可播放音频（加密或错误页），可把
  `file_quality` 降一档（如 `320k`）后重试。

## v0.11.5 - 2026-09-18

### 新增

- **歌曲分享自动识别**：群里有人分享 **QQ音乐 / 网易云音乐 / 酷狗音乐 / 酷我音乐** 的歌曲（QQ 的分享卡片，或直接把分享链接贴进聊天）时，插件自动把这首歌发出来（默认**语音**）。解析优先用分享链接里的平台原生 id（QQ音乐 `songmid`、网易云数字 id、酷狗文件 hash）向后端 `/music/:id/info` **精确取曲目**，取不到再按「歌名 歌手」在同平台搜索，并按歌名/歌手重合度挑选最像的一首——聚合搜索下直接取第 1 条很容易选到翻唱、合集或同名曲。
  - 新增配置「分享卡片自动识别」（**默认关闭，需显式开启**——自动发消息属于「机器人主动说话」）、「分享发送方式与优先级」（默认 `record_link → text`，独立于普通点歌的 `send_modes`）、「分享识别音质」（默认 `320k`）；
  - 开关只控制**自动**发送；引用分享再发 `点歌` / `点歌文件` 是用户明确操作，开关关着也照常可用；
  - 白/黑名单与管理员豁免沿用点歌那套；记录 `trigger_type` / `selection_type` 写入 `share`，WebUI 统计显示为「分享识别」。
- **引用分享 + 指令下载文件**：引用那条分享再发 `点歌文件` / `网易点歌文件` / `QQ点歌文件` / `酷狗点歌文件` / `酷我点歌文件`（**不带歌名**）即可下载那首歌的文件本体（内嵌封面与歌词，音质与嵌入开关走 `file_quality` / `file_embed_metadata`）；引用分享再发 `点歌`（不带歌名）则按分享策略发送。

### 设计取舍

- **自动识别默认关闭**：机器人主动在群里发消息需要使用者显式同意，因此 `share_auto_play` 默认 `false`；要自动发语音得先在配置里打开。而引用分享 + 指令属于用户自己的操作，与开关无关。
- **只认这四家平台的音乐分享**：卡片里认不出平台（小程序、图文、视频分享等）一律不响应——那些卡片同样带 `title`，只看「有歌名」就动手等于给每个分享都搜一遍歌；未配置 `api_base_url` / `api_key` 时静默跳过，不在「每条消息都会走」的钩子上刷配置提示。
- **识别成功才终止事件**：分享已处理就不再交给 AI，避免「先发语音、AI 再复读一句」；认不出时保持原样，命令与 AI 聊天不受影响。
- **失败不弹候选列表**：分享是「自动说话」，解析不到直接一句「识别到分享的《xx》，但没能在音源里找到」；搜索阶段也不复用「换个关键词试试吧」这类与分享场景对不上的通用提示。
- **消息段类型判断不用裸 isinstance**：插件与框架的 astrbot 模块可能不是同一对象（v0.10.5 的自动撤回、音乐卡片就这样静默失效过），因此按 `isinstance → 类名 → type 字段` 三级判断，另兜底支持少数客户端用 OneBot `music` 消息段下发分享的形态。

## v0.11.4 - 2026-09-13

### 修复

- **语音模式（`record_link` / `record_local`）在 NapCat 上常报「文件太大」**：AstrBot 的 OneBot 适配器对 `Record` 一律 `convert_to_base64()`，且内部 `MediaResolver` 会**强制把音频转成 wav** 再编码——体积因此只由时长决定，经 base64 膨胀 33% 后很容易超过协议端上限；把 `file` 换成网链同样无效（框架会先下载再转 wav）。现改为**经协议端直发**：`record_link` 直接下发链接，`record_local` 把已下载的音频注册为 AstrBot 文件服务链接（`/api/file/<token>`，该路由在 dashboard 鉴权白名单内、免登录、token 保护、带超时）后交由协议端自行拉取，**全程不经过 base64**。未配置 AstrBot 全局「对外可达的回调接口地址」时退化为 `file://` 本地路径（协议端与 AstrBot 同机可用）；任一环节失败都会自动回退原有 `Record` 发送方式，不会导致发不出。
- 新增配置项「语音模式经协议端直发（规避 base64 体积限制）」（默认开启），并同步 WebUI 配置页分组。

### 说明

- 本次只调整语音（`record_*`）发送链路：`card` / `file_link` / `file_local` / `text` 未改动（`File` 组件本就不走 base64，框架会转成 `file://` URI）。
- 若开启直发后仍报「文件太大」，说明卡的是语音文件体积/时长本身（QQ 对语音另有硬限制），建议改用 `file_local`（文件消息原样传输、不损失音质）。

## v0.11.3 - 2026-09-12

### 改进

- 配置页的「重载 / 保存并生效」固定到**右下角悬浮条**：配置项较多、页面很长，此前改完靠后的配置必须滚回顶部才能保存。用 `position: fixed` 而非 sticky，因为真正的滚动容器是控制台的 `.content` 区域，sticky 会被卡片自身的 `overflow` 裁掉。

### 修复

- **WebUI 白/黑名单文本域无法输入空格与换行**（连第二个号码都填不进去）：v0.11.2 的文本域把「解析后的数组」重新 join 回去当作受控值渲染，而换行 / 逗号 / 空格本身都是分隔符——刚敲下就被归一化吃掉，如同被施了魔咒。现改为编辑期间原样保留**原始输入**（`listText`），仅在保存与「当前 N 项」统计时才解析；逐行输入、逗号分隔、整段粘贴三种方式均可正常编辑。

## v0.11.2 - 2026-09-12

### 改进

- WebUI 配置页的**白名单 / 黑名单改为多行文本域**（一行一个，也支持逗号或空格分隔），下方实时显示「当前 N 项」，填完就能确认有没有被正确解析。此前它们被渲染成**单行输入框**：既不知道格式该怎么写，也看不到填了什么、解析成了几项。
- 名单类配置解析加固（新增 `core/config.py::parse_str_list`）：配置若被存成字符串（单行输入框保存、AstrBot 配置页或手工编辑 config），不再被**逐字符**拆开成 `["1","0","0",...]`——症状是名单看着填了却完全不生效，且毫无提示。现已兼容换行 / 逗号 / 顿号 / 分号 / 空格分隔，`AccessController` 与 `PluginConfig` 共用同一套解析。
- WebUI 保存按 schema 类型规范化：名单类一律落库为字符串列表，杜绝脏数据写入配置。

## v0.11.1 - 2026-09-12

### 修复

- FLAC 歌词标签键名由小写 `lyrics` 改为大写 `LYRICS`：Vorbis Comment 规范推荐字段名全大写，部分播放器/工具只按大写查找；后端下载链路（lx_music_api）写的也是大写，两边现在一致。mp3 的 `USLT` 帧与 m4a 的 `©lyr` 原子本来即符合标准，未改动。

## v0.11.0 - 2026-09-12

### 新增

- **「点歌文件」指令族**（`点歌文件` / `网易点歌文件` / `QQ点歌文件` / `腾讯点歌文件` / `酷狗点歌文件` / `酷我点歌文件` / `咪咕点歌文件`，含 QQ 点歌的大小写变体）：流程与点歌完全一致（搜索 → 候选列表 → 回复序号 → 下载），但发送阶段固定走**本地文件**——下载音频后把标题/歌手/专辑/封面/歌词写进文件标签再作为文件发出。支持一次到位语法（`点歌文件 晴天 2`），候选列表显示方式、自动撤回、白黑名单、点歌记录统计全部沿用点歌那套（记录的 `command` 字段可区分是哪个指令）。
- 文件下载的**独立配置**（与普通点歌互不影响）：
  - `file_quality`（默认 `flac`）：文件下载期望音质，独立于 `default_quality`；超过 API Key 上限时仍自动收敛到允许的最高音质。
  - `file_embed_metadata`（默认开）：是否把封面/歌词等写入文件标签，独立于 `embed_metadata`。
- WebUI 配置页新增「文件下载（点歌文件 指令）」分组，与「点歌行为」分组区分开。

### 修复

- **音质可观测性**：取播放链接时记录完整链路「期望音质 → 实际请求 → 后端实得（上游取链 / 本地曲库）」，并分别告警两种情况——① 期望音质超出本 Key 上限、已被收敛后才请求；② 后端返回的音质与请求不一致（命中本地曲库时按曲库已有文件给链）。此前只有一句「已收敛」的日志，「配了音质却没生效」难以定位。
- **搜索音质收敛**：「点歌 / 查询」类搜索接口显式带的 `quality` 现在会先按本 Key 上限收敛。后端对超出上限的显式音质是 **422 拒绝**（不是静默降级），此前若配置音质高于 Key 上限，会连搜索结果都拿不到；「点歌文件」的搜索也改为使用它自己的 `file_quality`（原先错用了普通点歌音质）。
- **Key 限额缓存刷新**：通过 WebUI 保存配置后会后台重新自检一次，刷新「最高音质 / QPS」缓存。此前若在后端调整了该 Key 的音质上限（或更换了 Key），插件会继续按**过期上限**收敛音质，表现为「改了配置但音质不变」。

### 设计取舍

- 文件指令**不做链接降级**：`file_local` 失败即明确提示（「文件下载失败了…」），绝不用链接糊弄，保证「文件」语义；同理，文件模式下不再追加歌词图片（歌词已写进文件标签），候选列表文案改为「回复序号下载文件」。
- 实现上把两种发送策略收敛到 `core/sender.py::DeliveryOptions`（音质 / 发送方式 / 嵌入开关 / 失败提示），`options=None` 即普通点歌，行为与改造前完全一致。

## v0.10.5 - 2026-09-12

### 修复

- WebUI 配置页「选歌后撤回候选列表」与插件配置页不一致、开关保存后刷新又变回关闭：该键未加入 WebUI 后端的可编辑白名单（`_EDITABLE_KEYS`），导致读取时被过滤（前端拿不到值 → 按默认关闭渲染）、保存时被静默丢弃（不写入 AstrBotConfig）。现已加入白名单，读写恢复正常。
- 顺带加固：启动注册路由时校验 `_conf_schema.json` 顶层键与可编辑白名单的一致性，发现遗漏立即输出 warning（列出键名），避免新增配置项再次出现「WebUI 看不到 / 改了不生效」。
- **撤回候选列表失效的根因（图片菜单模式）**：候选列表图片经协议端直发时，图片消息段的 `file` 字段漏了 `base64://` 前缀（只放了裸 base64），协议端把它当成文件路径去查找，回 `ActionFailed retcode=1200 未知文件类型或路径不存在` → 直发失败降级通用发送 → 拿不到 message_id → 永远撤回不了（且原先只有一行不醒目的日志）。现按 AstrBot 官方实现补上 `base64://` 前缀。
- 另修平台判断：原先用 `isinstance(event, AiocqhttpMessageEvent)`——插件以包形式加载时，插件 `import` 的 astrbot 模块与框架运行时未必是同一个模块对象，isinstance 可能恒为 `False`，撤回分支会静默跳过。现改为鸭子类型判断（探测 `bot` 上有没有 `call_action`），与 astrbot-comfyui-anima 的做法一致；音乐卡片的平台判断同样去掉 isinstance（原先会一起静默失效、导致卡片恒定降级为语音/文件）。
- 同链路加固（协议端调用与响应解析过于脆弱，任何一环不符都会「消息照发但拿不到 message_id」）：
  1. 原只走 `event.bot.api.call_action`，而 AstrBot 官方 aiocqhttp 适配器用的是 `bot.call_action`；现优先 `bot.call_action`、回退 `bot.api.call_action`（新增 `core/onebot.py` 统一封装，音乐卡片发送同步复用）；
  2. 原只认扁平响应 `{"message_id": ...}`，协议端返回 `{"data": {"message_id": ...}}` 时同样取不到 id；现两种结构都兼容，字符串 id 自动归一为数字；
  3. 群号 / QQ 号统一归一为 int（部分协议端只认数字类型，传字符串会直接报错降级）；
  4. 直发成功但没拿到 id 时不再降级重发候选列表（原先会重复发一份）；
  5. 日志补全：撤回成功 info、失败 warning；候选列表发送时即记下「能否撤回」的结论，撤回失败时**同一条日志直接带出原因**（协议端响应原文 / 开关状态 / 平台能力），不必再去翻发送那一刻的旧日志，不再有静默路径。
  6. 兜底反查：实机日志（AstrBot 4.28 + aiocqhttp）证实「消息直发成功、但协议端响应里没有 message_id」时撤回彻底无从下手。现改为紧接着反查一次最近消息历史（`get_group_msg_history` / `get_friend_msg_history`），取「发送者是机器人自己、且 60 秒内」的最新一条作为撤回目标；`message_id` 键名也兼容 `msg_id` / `messageId`。拿不到机器人自身 QQ 号、或协议端不支持 history API 时安全放弃——宁可漏撤回，绝不误撤回他人消息。

## v0.10.4 - 2026-09-12

### 修复

- WebUI 接口报「Cannot operate on a closed database」且数据全空：插件重载（上传新包）后，旧实例 terminate 关闭了记录库连接，而 WebUI 路由的旧 handler 仍持有旧实例引用，查询全部打到已关闭的连接并被异常兜底吞成空列表。双保险修复：
  1. WebUI 路由改为模块级稳定包装，请求始终转发到**当前活跃**的插件实例（旧路由残留也无害）；
  2. 记录库连接支持自愈——被关闭后再访问自动重开同一库文件（数据同源）。
- 顺带修复「更新日志」接口未注册成功的问题（方法被误置于模块函数体内）。

## v0.10.3 - 2026-09-12

### 改进

- 统计默认范围改为「近一天」（趋势图按整点小时显示）。
- 数据可诊断性：任务页空状态显示记录库累计条数与库文件路径（SSE 快照同步携带）；「点歌自检」追加显示记录库路径——若接口返回空，可据此确认数据写入位置；远程旧记录已由 v0.10.2 的 ts 回填纳入统计。

## v0.10.2 - 2026-09-12

### 修复

- WebUI 统计数据全空：v0.6.0 之前的旧记录经迁移补列后 `ts` 为 NULL，会被所有带时间过滤的统计查询排除；打开记录库时现在会按 `created_at` 自动回填旧记录时间戳（本地时区，无法解析的行跳过），旧数据重新可见。
- WebUI 版本号显示旧值：版本号是前端构建时从 metadata.yaml 注入的，后端单独发版时不会更新；打包脚本现在会检测产物版本并自动重建（需要 Node.js，不可用时警告并继续）。

## v0.10.1 - 2026-09-12

### 修复

- 自动撤回候选列表不生效：平台判断误用了 `get_platform_name() == "aiocqhttp"`，而该值是用户在平台配置里自定义的名称（如 napcat / qq），导致恒不匹配、候选列表静默走普通发送且无任何日志。现改为按事件类型（isinstance AiocqhttpMessageEvent）判断，与音乐卡片逻辑一致；撤回失败日志从 debug 提升为 warning 便于排查。

## v0.10.0 - 2026-09-12

### 新增

- 点歌记录新增「接口实际耗时」（`process_ms`：搜索 + 取链接 + 下载 + 嵌入 + 发送的纯处理时间，**不含排队与等待用户选号**）；`total_ms` 语义明确为端到端总耗时（含排队与等待用户选号）；`search_ms`（搜索接口耗时）从此前未填写改为每次记录都写入。实时任务页表格新增「接口耗时」列（旧库自动迁移补列）。
- WebUI 配置页的群号 / 个人 QQ 白名单与黑名单改为多行文本框（一行一个，支持 `*` 通配），替代原来的单行逗号分隔输入。

### 修复

- WebUI 配置页缺少「选歌后撤回候选列表」开关（新配置项未加入前端渲染分组，v0.8.0 起即不可见）。

## v0.9.0 - 2026-09-12

### 新增

- 统计范围细化：今天 / 近一天 / 近三天 / 近一周 / 近 14 天 / 近一月 / 近 90 天 / 近一年；「今天」「近一天」的趋势图按整点小时分桶，「近一年」按月分桶，其余按日期分桶；统计页新增刷新按钮；概览卡片与分布图跟随所选范围。
- 控制台导航栏固定（内容区独立滚动），标题旁显示版本号，点击弹出完整更新日志（后端新增 changelog 接口）。
- 界面换装萌系粉色主题（渐变背景、圆角卡片、粉紫渐变标题）。

### 修复

- 实时任务页「实时推送」未生效的问题：SSE 订阅失败（如旧版 AstrBot 不支持）时异常被吞、连轮询降级也未执行；现在建立失败会自动降级为 2 秒轮询，并以标签显示当前推送模式（实时推送 / 轮询 2s / 未开启）。
- 实时任务页「方式」列与统计分布图的枚举值中文化（音乐卡片 / 语音链接 / 本地文件 / 回复序号等，后端统一转换）。

## v0.8.0 - 2026-09-12

### 新增

- 候选列表自动撤回（新配置 `recall_candidate`，默认开启）：用户选完歌（或取消 / 序号超范围 / 超时）后自动撤回之前发送的候选列表消息，减少群内刷屏。仅 QQ(aiocqhttp) 平台可用（经 OneBot delete_msg），其他平台自动忽略；WebUI 配置页可开关。

## v0.7.5 - 2026-09-12

### 修复

- WebUI 配置页保存报「postMessage ... could not be cloned」：Vue reactive 代理对象无法被 bridge 的结构化克隆传输，保存前先转为纯 JSON 对象。
- 配置页滑块（数值类配置）右侧现在实时显示当前数值（滑块与数字输入框联动），并移除无效的 marks 属性。

## v0.7.4 - 2026-09-12

### 改进

- WebUI「音源」列更名为「平台」，内容从平台短码（wy/kw/tx…）改为中文名（网易云 / 酷我 / QQ音乐…），空值显示「-」；统计页「音源分布」同步更名为「平台分布」并使用中文名。

## v0.7.3 - 2026-09-12

### 修复

- WebUI 配置页报 500：插件实例缺失 AstrBotConfig 原始配置引用（`self.config`），读取与保存配置的接口一调用即报错；已补上并新增构造断言防回归。
- 控制台刷新后回到统计页：页签状态现在记入 sessionStorage，iframe 刷新 / 离开再进入时恢复上次所在页签（hash 会随 iframe 重载丢失，不可靠）。

## v0.7.2 - 2026-09-12

### 修复

- WebUI 实时任务页「歌曲」列显示 undefined：SSE 快照的查询漏了歌手 / 音源 / 来源 / 排队耗时等列，页面开着时表格每 2 秒被不完整数据覆盖；已对齐全部表格字段并加前端空值防御。

## v0.7.1 - 2026-09-12

### 修复

- 多人同时点歌时「没有找到相关歌曲」误报：实测后端音源被连续请求短暂冷却时会返回空结果，搜索为空时现在会间隔 3 秒自动重试一次，仍为空才提示无结果（已实测验证后端该行为）。
- 点歌并发默认值改为 1（严格串行）：上一个任务完成后再执行下一个，避免连续请求互相挤兑后端音源；需要并行可在配置调大（1-5）。

### 改进

- 等待选号期间再次发起点歌，不再被静默忽略：现在会提示「您还有一单点歌在进行中，请回复序号选择，或回复取消后再点新的」，不占用任务队列。

## v0.7.0 - 2026-09-12

### 新增

- WebUI 控制台（AstrBot 插件 Pages 机制，后台插件拓展页打开「萌音控制台」）：
  - 统计页：总量 / 今日 / 成功率汇总卡片，近 N 天搜索与点歌趋势折线，音质 / 发送方式 / 选歌方式 / 音源分布饼图，用户 / 群 / 歌曲排行条形图，排队与全流程平均耗时、音质降级率；
  - 实时任务页：任务队列状态（并发 / 执行中 / 等待中 / 提交 / 拒绝），最近点歌与搜索记录表格，SSE 实时推送（不支持时自动降级 2 秒轮询）；
  - 配置页：按配置 schema 结构化渲染全部插件配置，保存即写回并热更新（队列并发等结构性配置重启生效）。
- 后端新增 WebUI API（统计 / 任务 / 配置 / SSE 十组路由），前端源码在 `webui/`（Vue3 + Naive UI + ECharts，单文件构建到 `pages/moe-console/`）。

## v0.6.0 - 2026-09-12

### 新增

- 点歌任务队列：多用户同时点歌时按「点歌并发数」（`queue_concurrency`，默认 2）排队处理，防止高峰期请求与下载无序堆积；等待队列有上限（`queue_max_pending`，默认 20），队满时新请求直接提示繁忙而非无限堆积。排队等待时间一并写入记录（`queue_wait_ms`）。
- 记录库时间统计字段全面补齐（旧库打开自动迁移，数据无损）：
  - 时间双存储：`created_at`（ISO 本地时间）+ `ts`（unix 时间戳，统计排序/区间过滤用）；
  - 全链路耗时埋点（play_records）：`queue_wait_ms` 排队、`search_ms` 搜索、`resolve_ms` 取播放链接、`download_ms` 下载、`embed_ms` 元数据嵌入、`send_ms` 发送动作、`total_ms` 全流程；
  - 播放细节补充：`expires_at` 临时链接过期时间、`quality_requested` 期望音质与 `quality_fallback` 是否发生音质降级、`metadata_embedded` 元数据嵌入结果。
- 「点歌自检」回显任务队列状态（并发数 / 等待中 / 累计提交 / 累计拒绝）。

## v0.5.0 - 2026-09-12

### 新增

- 新配置「临时链接对外地址」（`public_base_url`）：后端按访问它的地址签发临时链接，机器人经局域网 / localhost 访问后端时，音乐卡片、语音、文件、文本链接里的地址群友无法访问——配置对外地址后（如 `https://music.example.com`，可带路径前缀），发送给用户的链接自动改写，而插件自己下载音频仍走后端原地址（内网高速下载不受影响）。后端地址本身对外可达时留空即可。

## v0.4.0 - 2026-09-12

### 新增

- 本地文件模式支持元数据嵌入（新配置 `embed_metadata`，默认开启）：发送前把标题 / 歌手 / 专辑 / 封面图片 / 歌词写入音频文件标签，播放器可直接显示封面与歌词。支持 mp3（ID3v2.3）/ flac / m4a，其余格式自动跳过；封面或歌词获取失败时静默降级、文件照常发送。新增依赖 mutagen（AstrBot 安装插件时自动装）。

## v0.3.2 - 2026-09-12

### 修复

- 文件模式发送的文件名去掉尾部 8 位随机后缀：现在是干净的「歌名 - 歌手.mp3/flac」样式（目录级随机隔离已足够防冲突）。

## v0.3.1 - 2026-09-12

### 修复

- 开启「点歌后附带歌词」（`enable_lyrics`）不生效的问题：v0.3.0 及之前版本该配置仅被解析但未实现，点歌成功后不会追加歌词；现点歌成功后会静默附加歌词图片（歌词获取失败或为空时只记日志，不打扰用户）。
- 歌词图片残留平台自定义元信息标签的问题（如酷我的 `[kuwo:127]`、`[ver:v1.0]`）：标签清理规则改为通用的「字母开头键:值」匹配，行中出现的标签也一并清理，时间轴不受影响。

### 文档

- 配置面板与 README 补充各发送方式的音质说明：语音消息（record）会被 QQ 协议强制转为 24kHz 单声道 SILK 语音编码，音乐听感模糊属协议限制；追求音质建议使用音乐卡片（card）或文件（file）模式。

## v0.3.0 - 2026-09-12

### 新增

- 搜索与点歌记录持久化（SQLite，存于 AstrBot 数据目录 `data/moe_music/records.db`）：记录 QQ 号、昵称、群号、群名、平台、私聊/群聊、管理员标记、关键词、命中数量、耗时、错误码、曲目信息、实际音质与发送模式、选歌方式（命令序号 / 单曲直发 / 回复序号 / AI 点歌）等完整字段，并按时间 / 用户 / 群 / 曲目建索引，为后续统计功能做数据积累。当前只记录不查询。
- 点歌白名单 / 黑名单访问控制：两者只能生效一个，白名单优先（白名单任一非空即启用并整体覆盖黑名单）；均区分群号名单与个人 QQ 名单，群号判断优先于个人（群不在白名单时，个人在白名单也无法在该群点歌）；管理员（AstrBot 管理员）不受任何限制；两个名单都为空时不限制。支持 `*` 通配。

## v0.2.0 - 2026-09-11

### 新增

- 候选列表显示方式配置（`selection_display`）：文本列表 / 图片菜单（带封面、序号、平台、时长），图片渲染失败自动回退文本。
- 「点歌 晴天 99」等超范围序号会提示后展示候选列表，而非静默忽略。

### 修复

- API Key 配置项在 Dashboard 配置面板中不可见的问题：原先使用 `invisible` 标记，AstrBot 面板对 invisible 项完全不渲染导致无法配置，现改为普通配置项（明文显示，与平台密钥等配置一致）。

## v0.1.0 - 2026-09-11

### 新增

- 首个版本发布（初始发布）：点歌命令（含平台别名与一次到位语法）、候选列表与序号选歌、音乐卡片 / 语音 / 文件 / 文本多模式降级发送、查歌词（图片渲染 + 文本回退）、点歌自检、LLM Tool（AI 自然语言点歌与查歌词）。
- 对接自建 lx_music_api 开放 API（/api/v1）：API Key 鉴权、429 限流退避重试、QPS 节流、音质超限自动收敛。
- 用户侧统一「温馨提示」文案，技术细节全部进 AstrBot 日志并对 API Key 脱敏。
