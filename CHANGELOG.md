# 更新日志

本文件记录 astrbot_plugin_moe_music 的版本变更，最新版本在最上方。

## v0.3.2 - 2026-09-12

### 修复

- 文件模式发送的文件名去掉尾部 8 位随机后缀：现在是干净的「歌名 - 歌手.mp3/flac」样式（目录级随机隔离已足够防冲突）。

## v0.3.1 - 2026-09-12

### 修复

- 开启「点歌后附带歌词」（`enable_lyrics`）不生效的问题：v0.3.0 及之前版本该配置仅被解析但未实现，点歌成功后不会追加歌词；现点歌成功后会静默附加歌词图片（歌词获取失败或为空时只记日志，不打扰用户）。
- 歌词图片残留平台自定义元信息标签的问题（如酷我的 `[kuwo:127]`、`[ver:v1.0]`）：标签清理规则改为通用的「字母开头键:值」匹配，行中出现的标签也一并清理，时间轴不受影响。

### 文档

- 配置面板与 README 补充各发送方式的音质说明：语音消息（record）会被 QQ 协议强制转为 24kHz 单声道 SILK 语音编码，音乐听感模糊属协议限制；追求音质建议使用音乐卡片（card）或文件（file）模式。

## v0.3.0 - 2026-09-12

### 新增

- 搜索与点歌记录持久化（SQLite，存于 AstrBot 数据目录 `data/moe_music/records.db`）：记录 QQ 号、昵称、群号、群名、平台、私聊/群聊、管理员标记、关键词、命中数量、耗时、错误码、曲目信息、实际音质与发送模式、选歌方式（命令序号 / 单曲直发 / 回复序号 / AI 点歌）等完整字段，并按时间 / 用户 / 群 / 曲目建索引，为后续统计功能做数据积累。当前只记录不查询。
- 点歌白名单 / 黑名单访问控制：两者只能生效一个，白名单优先（白名单任一非空即启用并整体覆盖黑名单）；均区分群号名单与个人 QQ 名单，群号判断优先于个人（群不在白名单时，个人在白名单也无法在该群点歌）；管理员（AstrBot 管理员）不受任何限制；两个名单都为空时不限制。支持 `*` 通配。

## v0.2.0 - 2026-09-11

### 新增

- 候选列表显示方式配置（`selection_display`）：文本列表 / 图片菜单（带封面、序号、平台、时长），图片渲染失败自动回退文本。
- 「点歌 晴天 99」等超范围序号会提示后展示候选列表，而非静默忽略。

### 修复

- API Key 配置项在 Dashboard 配置面板中不可见的问题：原先使用 `invisible` 标记，AstrBot 面板对 invisible 项完全不渲染导致无法配置，现改为普通配置项（明文显示，与平台密钥等配置一致）。

## v0.1.0 - 2026-09-11

### 新增

- 首个版本发布（初始发布）：点歌命令（含平台别名与一次到位语法）、候选列表与序号选歌、音乐卡片 / 语音 / 文件 / 文本多模式降级发送、查歌词（图片渲染 + 文本回退）、点歌自检、LLM Tool（AI 自然语言点歌与查歌词）。
- 对接自建 lx_music_api 开放 API（/api/v1）：API Key 鉴权、429 限流退避重试、QPS 节流、音质超限自动收敛。
- 用户侧统一「温馨提示」文案，技术细节全部进 AstrBot 日志并对 API Key 脱敏。
